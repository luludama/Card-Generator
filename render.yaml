services:
  - type: web
    name: card-generator
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: AI_PROVIDER
        value: openai
